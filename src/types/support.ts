export interface SupportNoticeData {
  link: string;
  title: string;
  date: string;
  new?: boolean;
}

export interface SupportFAQData {
  category: string;
  title: string;
  description: string;
}