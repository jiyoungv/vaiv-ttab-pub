export interface MyCommentData {
  link: string;
  comment: string;
  date: string;
  time: string;
  tabName: string;
}

export interface MyJoinTabData {
  link: string;
  title: string;
  subText: string;
  join: boolean;
}
