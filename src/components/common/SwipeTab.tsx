import { TabProps } from '@/components/common/Tab';

export interface SwipeTabProps extends TabProps {
}

export default function SwipeTab({
  ...props
}: SwipeTabProps) {
  return (
    <div className="bg-white">
      TODO: SwipeTab
    </div>
  );
}